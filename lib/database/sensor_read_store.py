import threading

class SensorReadStore(threading.Thread):
    def __init__(self, sensorReadFunction, dataStoreFunction):
        pass
        
    def run():
        # 1. Run and save value from "sensorReadFunction"
        # 2. Store by doing some formatting and passing as the only argument
        #    to "dataStoreFunction"
        
        pass